# react-native-phone-number-input pulls ancient nested deps (react-art@16,
# react-dom@16) that declare a React 16 peer, which conflicts with React 19.
# Tolerate that legacy peer range so installs succeed.
legacy-peer-deps=true
